import { TokenPrefix } from "../../../types/function/setToken/tokenPrefix"

const tokenPrefix: TokenPrefix = {
    access: "access-token",
    refresh: "refresh-token",
    confirmation: "confirmation",
    forgotPassword: "forgot-password",
    unlock: "unlock"

}

export { tokenPrefix }